<%= form_with(model:@blog, local:true) do |form| %> <!--％＝がいる-->
  <div class = "blog_content">
    <%= form.label :content %>
    <%= form.text_field :content %>
  </div>
    <%= form.submit "投稿" %>
<% end %>

<%= link_to "一覧に戻る", blogs_path %>

